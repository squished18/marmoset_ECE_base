The file can be generated by grades.cs.umd.edu via the 'Download Scores' link.
The format for this file is:
<p>
<pre>Last,First,UID,section,ClassAcct,DirectoryID,...
...
Last,First,UID,section,ClassAcct,DirectoryID,...
</pre>
You can directly upload the file as produced by grades.cs.umd.edu; this means that
you do <b>not</b> need to remove the first two lines of "header" information from the file,
nor do you need to remove the score or any other trailing fields after the DirectoryID.