#include "vigenere.h"
#include <stdio.h>
#include <stdlib.h>

/*
 * NOTE:  You MUST name this file "vigenere.c" or the Makefile on the server
 * will not work.
 *
 * Yes, this is moderately annoying when you want to name your file
 * "franzilcapzinfwupth.c" but that's just how it.  Just call it
 * vigenere.c and we'll be done with it.
 */


char* encrypt(char *plainText, int textLen, char *key, int keyLen)
{
    return plainText;
}


char* decrypt(char *cipherText, int textLen, char *key, int keyLen)
{
    return cipherText;
}


